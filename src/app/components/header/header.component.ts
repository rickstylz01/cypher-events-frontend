import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  templateUrl: './header.component.html',
  styleUrls: ['./header.component.css']
})
export class HeaderComponent {
  // create a property that represents the authentication state.
  //when the user logs in
  // set the property to 'true'

  // call setAuthenticated() method after successful login to set isAuthenticated to true

  // Implement logout logic 
  // I.E. clear user data from localStorage, etc.
}
